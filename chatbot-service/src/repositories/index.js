const chatSessionRepository = require("./chatSession.repository");
const knowledgeBaseRepository = require("./knowledgeBase.repository");

module.exports = {
  chatSessionRepository,
  knowledgeBaseRepository,
};
